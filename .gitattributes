# Treat generated weather datasets as data, not reviewable source text.
*.clm -diff
*.ddy -diff
*.epw -diff
*.pvsyst -diff
*.rain -diff
*.stat -diff
*.wea -diff
static/data/*.json -diff
# The aggregated news feed is regenerated by the deploy workflow, so its
# one-line payload is not reviewable source text.
static/news.json -diff
